version: '3.8'

services:
  app:
    image: my-app-image
    build:
      context: .
    environment:
      - var-run=runner2
      # Измени на runner2 для другого раннера

#version: "3.8"

#services:
#  hello:
#    image: alpine
#    container_name: hello-world-container
#    command: echo "Hello, $GREETING_TARGET!"
#    environment:
#      - GREETING_TARGET=${GREETING_TARGET}
#      - Var-run=runner2
